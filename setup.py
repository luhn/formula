
from setuptools import setup, find_packages

setup(
    name='formula',
    version='1.0',
    description='',
    author='Theron Luhn',
    author_email='theron@luhn.com',
    packages=find_packages(),
    requires=[
    ],
    )
